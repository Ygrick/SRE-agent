[project]
name = "ai-sre-platform"
version = "0.1.0"
description = "AI-SRE Platform — infrastructure + SRE agent"
requires-python = ">=3.12"
dependencies = []

[tool.uv]
dev-dependencies = [
    "qdrant-client>=1.12.0",
    "sentence-transformers>=3.0.0",
    "locust>=2.0.0",
]
